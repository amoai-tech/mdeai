# Product wireframes

Current design-reference wireframes migrated from the former top-level `docs/wireframes/` tree.

These files are design references, not live implementation status. Verify routes and behavior against current `src/app` and canonical domain documentation before implementation.
