# Domains

Product-domain documentation.

Each active domain should keep only the documentation it actually needs. Typical files are `README.md`, `architecture.md`, `user-journeys.md`, `data-model.md`, and `operations.md`.

Do not create empty template files merely to fill the structure.
