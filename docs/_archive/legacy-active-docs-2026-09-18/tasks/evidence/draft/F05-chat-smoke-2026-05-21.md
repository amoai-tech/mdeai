# F05 chat smoke — Chrome DevTools MCP (2026-05-21)

**Environment:** `npm run dev` · UI http://localhost:3001 · Mastra http://localhost:4111  
**Stack:** Next.js 16.2.6 · CopilotKit 1.55.2 · Mastra 1.1.0-alpha.3 · `@ai-sdk/google@2.0.74`  
**Agent:** pingAgent

## RESULT

**PASS** — full browser chat cycle works; zero console errors; all `/api/copilotkit` 200.

## Probes

| Probe | Result |
|-------|--------|
| `GET /` | 200 · title `mdeai — concierge for Medellín` |
| `GET :4111/` | 200 Mastra Studio |
| Copilot sidebar | Renders · `mdeai concierge` · initial day-1 message |
| Send `hello` | User bubble + agent reply streamed · Stop→Send transition OK |
| Send `ping` | User bubble + `Pong! The wiring is alive and well.` |
| Console errors | **0** |
| Console warnings | 1× Lit dev mode (CopilotKit web-inspector) |
| Failed network (localhost) | **0** |
| `POST /api/copilotkit` | All **200**; chat turns ~3.0–3.4s application time |
| AI SDK v4/stream error | **None** (after `@ai-sdk/google` ^2.0.42) |
| `INCOMPLETE_STREAM` | **None** |
| Hydration / React crash | **None** (`nextError: null`) |

## Exact message note

Gemini replies are **non-deterministic**. This session:

- `hello` → `Hello! Connection confirmed. The wiring is alive and fully operational!`
- `ping` → `Pong! The wiring is alive and well.`

Prior session (`ping — full cycle test`) returned `Pong! The wiring is fully alive and operational.` — same intent, different wording. Not a runtime defect.

## CopilotKit init POSTs

7× `POST /api/copilotkit` on page load (handshake/state sync) — all complete in &lt;35ms except chat turns. No stalled requests; no polling loop observed.
