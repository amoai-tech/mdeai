# Copy to `supabase/functions/.env` (gitignored as `.env`). Restart the stack:
#   supabase stop && supabase start
# Match this value when exporting for Deno (or source this file before `deno test`).
RULES_ENGINE_SECRET=local-rules-engine-dev-secret
