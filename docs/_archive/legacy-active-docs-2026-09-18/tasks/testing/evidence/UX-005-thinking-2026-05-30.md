# UX-005 thinking indicator — browser evidence (2026-05-30)

**Branch:** `feat/ux-002-005-chat`  
**Fix:** UX-005 Fix 3 — `concierge-pending-store` + dual render (Input + Messages)

## Smoke command

```bash
cd mdeapp && npm run dev   # :3001 + Mastra :4111
node scripts/smoke-ux005-thinking.mjs
```

## Result

| Check | Result |
|-------|--------|
| `thinkingCaught` | **true** |
| `inProgressCaught` | **true** |
| Query | Metro/Laureles (agent path — avoids event chip fast-path) |
| Screenshot | `tasks/testing/evidence/ux-005-thinking-smoke.png` |

## Unit tests

```text
npm run test -- src/lib/__tests__/concierge-pending-store.test.ts src/components/chat/__tests__/
→ 16/16 PASS
npm run typecheck → PASS
npm run lint → PASS
```

## Notes

- Event/rental chip fast-path bypasses CopilotKit `inProgress` — use non-search queries for UX-005 proof.
- Prior audit fail was React 19 batching + `canStop`-only guard; fixed with pending store.
