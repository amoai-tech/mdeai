# VEN-031 / SAN-314 verify — 2026-06-03

## Scope

Unified venue release Playwright gate — `e2e/screens/VEN-035-venue-release.spec.ts` + `e2e/helpers/venue-release.ts`.

**Linear:** [SAN-314](https://linear.app/sanjiovani/issue/SAN-314)

## What shipped

| Area | Coverage |
|------|----------|
| SCREEN-023 | `/restaurants` grid, Laureles filter, back to chat |
| SCREEN-028 | `/cafes` placeholder → chat |
| SCREEN-022 browse shell | `/nightlife` placeholder → chat |
| SCREEN-018 | Mobile FAB, map sheet cycle, composer usable |
| SCREEN-021 | Café cards, detail panel, curated fallback assertions |
| SCREEN-022 chat | Nightlife cards, panel (not café tabs) |
| Hygiene | No duplicate lists, console pageerror sweep |
| SAN-368 | `EXPECT_CURATED_GROUNDING_FALLBACK=true` — Google-verified candidate, no ADK attribution |

**Strict:** no agent nudge retry helpers; 90s card timeout; serial suite.

## Verification

| Command | Result |
|---------|--------|
| `npm run lint` | pass |
| `npm run typecheck` | pass |
| `npm run test:e2e:venue-release` | **8/8 pass** (chromium, fresh `dev:ui` on :3001) |
| `npm run verify:task -- VEN-031 --skip-floor` | **PASS** — 22 vitest + 8/8 Playwright (2026-06-03) |
| `npm run floor` | **PASS** (2026-06-03) |
| **CI** [PR #60](https://github.com/amo-tech-ai/mdeapp/pull/60) | **green** — floor ✅ · CodeRabbit ✅ · Vercel preview ✅ (2026-06-03) |
| **Merge** | Squash to `main` via PR #60 (`2d9691f` → post-merge SHA on `origin/main`) |

**Note:** Stale dev server on :3001 without `/restaurants` returns 404 — kill port or let Playwright boot `dev:ui`. Mastra agent required for chat tests (`npm run dev:agent` on :4111).

### verify:task probe log (2026-06-03)

```
vitest src/components/venues/  → 3 passed
vitest src/lib/venues/         → 19 passed
playwright VEN-035-venue-release → 8 passed (~1.4m, serial chromium)
```

**Skills routed (index-skills.md):** `task-verifier` · `testing` · `playwright-cli` — no MCP required for TEST layer (VEN-VERIFY-MATRIX § VEN-031).

## Not in scope (follow-ups)

- Signed-in booking persist e2e (VEN-021 auth fixture)
- Full `/nightlife` / `/cafes` browse (SAN-491 / SAN-519)
- Real ADK grounding assertions (flip `EXPECT_CURATED_GROUNDING_FALLBACK` when SAN-368 Done)

## Grade

**A / 92 — PR slice shipped** · **SAN-314 not Done** (task-verifier)

| Dimension | Score | Notes |
|-----------|------:|-------|
| Spec alignment | 85 | Unified release suite; legacy per-screen specs optional cleanup |
| Disk + CI proof | 98 | 8/8 e2e · 22 vitest · verify:task · floor · PR #60 CI green · merged |
| DoD gaps | −10 | Signed-in booking persist e2e still missing |
| SAN-368 | hold | `EXPECT_CURATED_GROUNDING_FALLBACK=true` until ADK prod |

**Merge-ready:** ✅ PR #60 merged to `main`.  
**Not Done:** signed-in booking e2e · SAN-368 flip · optional prod smoke row.
