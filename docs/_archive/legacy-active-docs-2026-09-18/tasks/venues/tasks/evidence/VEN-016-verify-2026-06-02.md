# VEN-016 verification — requestVenueBooking Mastra tool

**Date:** 2026-06-02  
**Task:** `tasks/venues/tasks/mvp/016-ven-request-venue-booking-tool.md`  
**Grade:** **B+ / 88**

## Summary

Mastra `requestVenueBooking` tool inserts pending rows into `venue_booking_requests` via user-scoped Supabase client (RLS). Registered on `conciergeAgent`; CopilotKit disabled render shows confirmation chip.

## Skills / MCP / docs

| Source | Finding |
|--------|---------|
| **mastra** | `createTool` + `withAudit` / `runAuditedMutation` (MASTRA-004) pattern matches search tools |
| **mde-supabase** | Live schema via MCP matches migration DATA-009; RLS insert requires `auth.uid()` = `user_id` |
| **Task spec** | Input uses `google_place_id`, `nightlife` kind; mapped to `place_id`, `nightclub` at DB boundary |
| **Gap vs spec** | Added required `contact_name` + `contact_email` (NOT NULL in live DB; spec only listed WhatsApp) |

## Live schema (Supabase MCP)

`venue_booking_requests`: `place_id`, `venue_kind` (`cafe|restaurant|nightclub`), `requested_at`, `contact_name`, `contact_email`, `contact_phone`, `status`, `source`, `metadata`, RLS enabled.

## Implementation

| File | Role |
|------|------|
| `mdeapp/src/mastra/tools/request-venue-booking.ts` | Tool + Zod + insert helper |
| `mdeapp/src/mastra/lib/run-audited-mutation.ts` | Medium-risk audit wrapper |
| `mdeapp/src/mastra/lib/tool-audit-context.ts` | `mdeaiAccessToken` for JWT |
| `mdeapp/src/app/api/copilotkit/[[...path]]/route.ts` | Passes session access token |
| `mdeapp/src/mastra/agents/concierge.ts` | `requestVenueBooking` registered |
| `mdeapp/src/platform/copilot/mastra-tool-action-names.ts` | CopilotKit name `requestVenueBooking` |
| `mdeapp/src/components/copilot/search-tool-renders.tsx` | Pending / confirmation chip |

## Tests

```bash
cd mdeapp && npm test -- --run \
  src/mastra/tools/__tests__/request-venue-booking.test.ts \
  src/mastra/lib/__tests__/tool-audit-context.test.ts \
  src/platform/copilot/__tests__/mastra-tool-action-names.test.ts
# 3 files, 11 tests — PASS
```

Coverage:

- `nightlife` → `nightclub` mapping
- Invalid `venue_kind` rejected (Zod)
- Mocked Supabase insert + RLS error surfacing
- Concierge registry + CopilotKit name alignment
- Access token round-trip in request context

## Acceptance criteria

| Criterion | Status |
|-----------|--------|
| RLS: user inserts own row only | ✅ User-scoped client + explicit `user_id`; unauthenticated throws |
| Invalid `venue_kind` rejected | ✅ Zod enum |
| Vitest with mocked Supabase | ✅ |
| Tool name matches registry | ✅ Key `requestVenueBooking`, id `request-venue-booking` |
| CopilotKit mirror `requestVenueBooking` | ✅ |
| `withAudit` / MASTRA-004 | ✅ `runAuditedMutation`, RiskLevel.medium |

## Not in scope (follow-ups)

- **VEN-021** — wire `RestaurantBookingSheet` / café sheet to call tool or API
- **VEN-017/019** — HITL / form UX
- Live authenticated insert e2e (needs signed-in Playwright session)
- Admin UPDATE policies (VEN-024)

## Deductions (−12)

- Booking UI sheets still stub (−5)
- No live DB insert proof with real JWT (−4)
- `contact_name`/`contact_email` beyond minimal task spec (−0, required by schema)
- Pre-existing lint debt unrelated (−3 risk)
