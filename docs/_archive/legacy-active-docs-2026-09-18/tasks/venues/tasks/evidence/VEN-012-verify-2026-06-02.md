# VEN-012 verify — 2026-06-02

| Field | Value |
|-------|-------|
| Task | VEN-012 Grounded café vs nightlife kind split |
| Grade | **A- / 92** (post PR #48 unblock 2026-06-02 PM) |
| Status | In Review |
| Production ready | Pending merge approval |

## What shipped

- `search-grounded-places` emits `metadata.venueKind` (`cafe` \| `nightlife` \| `general`) on every return path
- `filterNightlifeGroundingRows` + `resolveVenueGroundingKind` for query intent
- `parseGroundedToolResult` + `readGroundedVenueKind` for UI routing
- `GroundedPlaceResults` branches to `GroundedNightlifeResults` vs `GroundedCafeResults`
- `toNightlifeVenueDetail` + `openNightlifeDetail` (no hardcoded `kind: "cafe"`)
- Pin meta `venueKind` in `normalize-tool-output`
- `searchNightclubVenueAnchors` fallback for nightlife queries

## Verification

| Layer | Result |
|-------|--------|
| Vitest | **440/440** + classifier fast-path tests |
| Floor | **pass** (lint, tsc, build, test, audit) |
| GitHub PR #48 CI | Floor **pass**, Vercel **pass** (`a39d72c`) |
| Playwright | SCREEN-022 chromium **2/2 pass** (grounded fast path + sheet dismiss) |

## PR #48 unblock (same day)

- Removed `venueBookingToolRender` (VEN-019 scope)
- Restored inline `CafeResultCard` (missing `result-card-shell`)
- **Routing fix:** `looksLikeNightlifeGroundingSearch` — restaurant fast path no longer steals `rooftop cocktails` queries
- `GroundedFastPathPanel` → `GroundedPlaceResults` for nightlife cards

## Gaps

- Map pin category stays `grounded` (no new `nightlife` enum) — sub-kind via `meta.venueKind` + `data-result-kind`

## Files

- `mdeapp/src/lib/grounded-venue-kind.ts`
- `mdeapp/src/lib/parse-grounded-tool-result.ts`
- `mdeapp/src/mastra/tools/search-grounded-places.ts`
- `mdeapp/src/mastra/tools/search-venue-anchors.ts`
- `mdeapp/src/components/copilot/search-tool-renders.tsx`
- `mdeapp/src/platform/maps/normalize-tool-output.ts`
