# VEN-014 verification — Places cache + field masks

**Date:** 2026-06-02 (updated after PR #52)  
**VEN-014 cache PR:** #50 → `main` @ `aa1d5af`  
**VEN-014b guard PR:** #52 → `main` @ `435c476` · Linear **SAN-520** Done  
**Task status:** **In Review** — live miss→hit blocked on GCP Places 403  
**Grade:** **B+** — read-through + retry guard shipped; MAP-005 edge proxy still deferred  
**Production ready:** No (GCP Places key + miss→hit proof pending)

## Contamination check

| Check | Result |
|-------|--------|
| `git status` | Clean of mobile WIP; only VEN-014 paths |
| PR #49 `b1384c6` | Not on branch base |
| Service role | `createServiceRoleClient()` via allowed `src/lib/supabase/service.ts` only |

## Current flow (before → after)

| Step | Before | After |
|------|--------|-------|
| Tourist opens café/nightlife detail | `usePlaceDetails` → GET `/api/places/detail` | Same |
| API route | Always `getPlaceDetails()` → Google | `fetchPlaceDetailsWithCache()` — Supabase `place_details_cache` first |
| Places HTTP | Every panel open | Only on cache miss / expired / no service env |
| Field mask | `validatePlacesFieldMask` in `google-places-client` | Unchanged; version key `PLACE_DETAILS_FIELD_MASK_VERSION` on cache row |
| Response header | — | `X-Places-Cache: hit \| miss` |

## Files changed (mdeapp)

| File | Change |
|------|--------|
| `src/lib/place-details-cache.ts` | Read-through + upsert (7d TTL, composite PK) |
| `src/lib/place-details-cache.test.ts` | Cache hit/miss + mask version filter |
| `src/app/api/places/detail/route.ts` | Wire cache; expose `X-Places-Cache` |
| `src/components/cafe/cafe-detail-panel.tsx` | `place-details-unavailable` on enrichment error |
| `src/components/nightlife/nightlife-detail-panel.tsx` | Loading + error fallback copy |

## Cache behavior

- **Table:** `place_details_cache` (`place_id`, `field_mask_version`) — migration MAP-018E
- **Hit:** `expires_at > now()` AND `field_mask_version = details-v3-links-2026-05-26`
- **Miss:** `getPlaceDetails()` with MVP mask (no `*` — `validatePlacesFieldMask` throws)
- **Write:** upsert payload + `photo_name_primary`, `google_maps_uri`, lat/lng after fetch
- **No DB env:** degrades to direct Places (same as pre-VEN-014)

## Tests run

```bash
cd mdeapp
npm run lint                    # pass
npm run typecheck               # (run with floor)
npm run test -- --run src/lib/place-details-cache.test.ts src/mastra/lib/google-places-client.test.ts
# 15/15 pass
```

## Risks / blockers

| Risk | Mitigation |
|------|------------|
| Low cache hit rate (DATA-008 403 backfill) | Ops: fix Places key; seed/backfill script |
| MAP-005 edge proxy not built | This task uses Next API + service role (acceptable MVP per MAP-018E) |
| Duplicate panel opens still hit API if TTL expired | Expected; 7d TTL documented |
| Full `npm run floor` | ✅ pass 2026-06-02 (449 tests) |

## Acceptance (VEN-014 task file)

| Criterion | Status |
|-----------|--------|
| Server Places routes use FieldMask | ✅ existing + tests |
| Wildcard `*` blocked | ✅ `validatePlacesFieldMask` + vitest |
| Detail panels read cache before fetch | ✅ via `/api/places/detail` |
| Quota/error fallback in UI | ✅ `place-details-unavailable` |
| No browser Places server key for detail | ✅ unchanged |
| CI grep gate | 🟡 hook `places-api-field-mask.mjs` exists; not added to npm floor |

## PR #50 audit hardening (2026-06-02)

| Audit item | Status |
|------------|--------|
| Remove `as "restaurants"` cast | ✅ commit `2e5ad1e` |
| Generated `database.types.ts` | ✅ `supabase gen types` (project `zkwcbyxiwklihegjhuql`) |
| `createServiceRoleClient<Database>()` | ✅ |
| Unused `expired` reason | ✅ removed |
| `npm run floor` post-fix | ✅ pass |
| Live miss → hit headers | ❌ blocked — Places API **502** on localhost |

## Status recommendation

| Task | Verdict |
|------|---------|
| **VEN-014** | **In Review** — merge after live miss/hit proof when Places key works |
| **MAP-005** | Still **Not Started** for edge `places-proxy`; VEN-014 covers Details read-through slice |

## Next

1. Commit slice on `feat/ven-014-map-005-places-cache`
2. `npm run floor`
3. MAP-005 follow-on: edge proxy + `places_search_cache` (separate PR)
