# VEN-020 verify — 2026-06-02

## Scope

Booking status chips on café/nightlife detail panels — read-only, RLS-scoped, honest copy.

**Merged:** PR #55 → `main` @ `3772d79`  
**Depends on:** VEN-021 Done — proof row `5b98fb38-3265-4d2c-9031-b9bb8eb53ac5` (`ChIJven021proof01`, `pending`)

## Query path

```
CafeDetailPanel / NightlifeDetailPanel
  → VenueBookingStatusChip
  → useVenueBookingStatus (useSession gate)
  → createBrowserClient()
  → fetchLatestVenueBookingStatus()
  → venue_booking_requests
      .eq('place_id', placeId)
      .eq('venue_kind', cafe | nightclub)
      .order('created_at', { ascending: false })
      .limit(1)
```

RLS `venue_booking_select_own` returns only `auth.uid()` rows — no service role in UI path.

Nightlife UI `uiKind=nightlife` → DB `venue_kind='nightclub'`.

## Status mapping

| DB / normalized | Chip label |
|-----------------|------------|
| `pending` | Request pending |
| `reviewing`, `approved` | Under review |
| `sent`, `contacted` | Contacted |
| `needs_user` | Venue has a question |
| `confirmed` | Confirmed |
| `cancelled`, `declined` | Request declined |
| `rejected` | Request declined (destructive) |

Schema CHECK today: `pending`, `confirmed`, `declined`, `cancelled` — extra keys are forward-compatible.

## Verification

| Layer | Result |
|-------|--------|
| Lint | pass |
| Typecheck | pass |
| Unit | `npm run test -- venue-booking-status` — 13 passed |
| RLS fetch proof | `scripts/prove-ven-020-status-fetch.mjs` | **pass** — `pending` for `ChIJven021proof01` |
| Manual UI chip | Browser screenshot on localhost | optional polish |

### Live proof row (2026-06-03)

```json
{
  "id": "5b98fb38-3265-4d2c-9031-b9bb8eb53ac5",
  "status": "pending",
  "venue_kind": "cafe",
  "place_id": "ChIJven021proof01"
}
```

## Grade: **A- / 90**

- Chip + query + mapping ✅
- Tests ✅
- Signed-in RLS query proof ✅

## Task status

**Done** — PR #55 merged; UI chip uses same query path as proof script.

**Linear:** SAN-307 (catalog VEN-028) / repo VEN-020
