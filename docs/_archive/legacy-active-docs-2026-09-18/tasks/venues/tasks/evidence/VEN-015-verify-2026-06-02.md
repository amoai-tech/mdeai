---
task: VEN-015
date: 2026-06-02
status: Verified (partial)
grade: B+
execution_score: 85
mcp: plugin-supabase-supabase execute_sql
project: zkwcbyxiwklihegjhuql
---

# VEN-015 — `venue_booking_requests` schema + RLS verify

## MCP probes (2026-06-02)

### Table + RLS

```sql
SELECT relname, relrowsecurity FROM pg_class c
JOIN pg_namespace n ON n.oid = c.relnamespace
WHERE n.nspname = 'public' AND relname = 'venue_booking_requests';
```

| table_name | rls_enabled |
|------------|-------------|
| venue_booking_requests | **true** |

### Policies (live)

| polname | polcmd | roles |
|---------|--------|-------|
| venue_booking_insert_own | INSERT | authenticated |
| venue_booking_select_own | SELECT | authenticated |
| venue_booking_service | ALL | service_role |

### Columns (live — 18)

Core: `id`, `user_id`, `venue_kind` (cafe|restaurant|nightclub), `place_id`, `restaurant_id`, `venue_anchor_id`, `party_size`, `requested_at`, contact fields, `status`, `source`, `idempotency_key`, `metadata` jsonb, timestamps.

### Migration on disk

`mdeapp/supabase/migrations/20260529234934_data009_venue_booking_requests.sql` — applied live per migration header.

## Acceptance matrix

| Criterion | Status | Notes |
|-----------|--------|-------|
| Migration applies | ✅ | Live table matches migration |
| RLS enabled | ✅ | MCP confirmed |
| User SELECT/INSERT own rows | ✅ | `user_id = (SELECT auth.uid())` |
| User UPDATE own rows | ❌ | No authenticated UPDATE policy yet |
| Admin / Patricia policies | ❌ | Deferred to VEN-024 admin queue |
| Negative cross-user smoke | ⚪ | Not run this session — needs two auth fixtures |
| `whatsapp_draft` column | ⚪ | Stored in `metadata` jsonb per DATA-009 design |

## Verdict

**Schema verify: PASS** — unblocks **VEN-016** (`requestVenueBooking` tool).

**RLS hardening: PARTIAL** — Patricia admin + UPDATE + penetration tests remain **VEN-025 / VEN-024**.

## Grade (B+ / 85)

Ship-ready for app-layer booking insert; audit checklist items for admin UPDATE and negative smoke still open.

## Next

1. **VEN-016** — Mastra `requestVenueBooking` tool
2. **VEN-017/019/021** — booking sheet + HITL + persist
3. **VEN-024** — admin SELECT/UPDATE policies for Patricia
