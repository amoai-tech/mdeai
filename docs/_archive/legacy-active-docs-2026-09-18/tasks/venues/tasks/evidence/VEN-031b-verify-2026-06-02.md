# VEN-031b verification — SCREEN-021 ask-prompt e2e

**Date:** 2026-06-02  
**SHA:** local main (post-fix, uncommitted)  
**Grade:** **A-** (spec + fix correct; MCP docs unavailable this run)

## Skill / docs audit

| Source | Verdict |
|--------|---------|
| `copilotkit` router | Correct — routes debug + develop for DOM/thread issues |
| `copilotkit-develop` | `useCopilotChat` + `appendMessage(TextMessage)` in `CafeDetailPanel` matches v1.55.2 pattern — **keep** |
| `copilotkit-debug` | Symptom = dual render paths (runtime thread + `EventLocalChat`), not runtime connectivity |
| CopilotKit MCP `search-docs` | Not connected this session — relied on disk (`@copilotkit/react-ui` `UserMessage`, `ConciergeChatMessages`) |

## Root cause (confirmed)

1. Café grounded **fast path** calls `showExchange()` → local user/assistant bubbles render **after** CopilotKit messages in `ConciergeChatMessages`.
2. E2E used `.copilotKitUserMessage.last()` → last bubble was `waitForCafeGroundedCards` retry nudge, not `appendMessage` from ask-prompt.
3. `appendMessage` was already working; assertion was wrong.

## Fix (minimal)

| File | Change |
|------|--------|
| `e2e/screens/SCREEN-021-cafe-listings.spec.ts` | Assert `filter({ hasText })` on `concierge-user-message` **or** `.copilotKitUserMessage` inside `copilot-chat-region` |
| `src/components/chat/concierge-chat-messages.tsx` | `ConciergeUserMessage` wrapper + `data-testid="concierge-user-message"` on local fast-path user bubbles |

**Not changed:** `cafe-detail-panel.tsx` — `appendMessage` remains the correct CopilotKit API for follow-up turns.

## Verification

```bash
cd mdeapp
npm run lint          # pass
PW_SKIP_WEBSERVER=1 npx playwright test e2e/screens/SCREEN-021-cafe-listings.spec.ts --project=chromium
# 4/4 pass (2026-06-02)
```

## Best-practice scorecard

| Practice | Score | Note |
|----------|-------|------|
| CopilotKit API choice | ✅ | `appendMessage` + `MessageRole.User` for detail ask-prompts |
| Fast path coexistence | ✅ | Local chat for zero-latency search; runtime for follow-ups |
| E2E stability | ✅ | No `.last()` on shared CSS class across dual sources |
| Test IDs | ✅ | `concierge-user-message` for both paths |
| Avoid agent bypass in test | ✅ | Did not disable fast path — tests real Camila path |

## Residual

- Short query `"salsa bars in Poblado"` may still hit **event** fast path before nightlife — out of VEN-031b scope (routing).
