# VEN-021 verify — 2026-06-02

## Scope

Wire café + nightlife booking sheets → `POST /api/venue-booking/request` → `venue_booking_requests` (`source=web`, `status=pending`, RLS `venue_booking_insert_own`).

**Merged:** `mdeapp` PR #53 → `main` @ `bf2599d`  
**Hotfix:** `crypto` import (not `node:crypto`) for Next webpack — required for API route on disk.

## Data path

```
CafeBookingSheet / NightlifeBookingSheet
  → VenueBookingForm (useSession gate)
  → submitVenueBooking() fetch POST /api/venue-booking/request
  → createClient() + auth.getUser()
  → insertVenueBookingRequest() → venue_booking_requests
  → setVenueBookingConfirmation → VenueBookingConfirmationBanner
```

Nightlife UI maps to DB `venue_kind = 'nightclub'`.

## Verification

| Layer | Command | Result |
|-------|---------|--------|
| Lint | `npm run lint` | pass (pre-merge) |
| Typecheck | `npm run typecheck` | pass (pre-merge) |
| Unit | `npm run test -- venue-booking` | 13 passed |
| Live insert | `node --env-file=.env.local scripts/prove-ven-021-insert.mjs` | **pass** |

## Insert proof (signed-in)

| Field | Value |
|-------|-------|
| **Row id** | `5b98fb38-3265-4d2c-9031-b9bb8eb53ac5` |
| **user_id** | `a5f6adc5-5e86-4730-8ade-3617893a4f68` |
| **venue_kind** | `cafe` |
| **place_id** | `ChIJven021proof01` |
| **status** | `pending` |
| **source** | `web` |
| **contact_email** | `qa-landlord@mdeai.co` |
| **created_at** | `2026-06-02T23:22:38.440382+00:00` |
| **API message** | Request sent — we'll confirm by WhatsApp. This is not a confirmed reservation yet. |

Probe: magic-link session (`qa-landlord@mdeai.co`) → `POST http://localhost:3001/api/venue-booking/request` → MCP/service row lookup by id.

## Grade: **A- / 92**

- Real signed-in insert + row id ✅
- Honest pending copy ✅
- Café + nightlife sheets ✅
- Restaurant sheet still absent (follow-up)
- VEN-026 duplicate UX polish optional

## Task status

**Done** — web persist spine shipped; agent HITL (VEN-019) and restaurant sheet remain separate tasks.

**Linear:** [SAN-304](https://linear.app/sanjiovani/issue/SAN-304)

## Follow-up

| Next | Why |
|------|-----|
| **VEN-028** status chips | DB rows exist — show pending / reviewing / contacted on detail panels |
| Hotfix PR | `crypto` import for webpack (post-#53) |
| VEN-026 | Surface 409 duplicate in UI |
