# SAN-890 · CK-V2-004 — Migrate /chat (conciergeAgent) to v2 — Forensic Spec Audit (05)

**Date:** 2026-06-12 · **Auditor:** senior software specialist + forensic verifier
**Question:** *Is SAN-890 · CK-V2-004 — Migrate /chat (conciergeAgent) to v2 100% correct?*
**Method:** every v1 API the spec names was grepped in its named file on `main` @ `0fab08f`; every v2 replacement hook was checked against the installed `@copilotkit/react-core@1.55.2` `/v2` export list (`dist/v2/index.d.cts`); the SAN-889 blocker gate, the tool-render count, and the file counts were verified on disk.
**Inputs:** Linear SAN-890 (full step list A–G), spike [`CK-V2-004-chat-subspike.md`](../tasks/copilotkit/CK-V2-004-chat-subspike.md), [`official-docs.md`](./official-docs.md), skill `.claude/skills/copilotkit-upgrade`.

---

## Verdict — first read

**SAN-890 · CK-V2-004 is ~96% correct — a GO, and it is now unblocked.** This is a **spec**, not code (zero `/chat` v2 files exist yet — correct, it's the last/highest-risk route). Every v1 API the plan says it must replace really is in the file it names, and every v2 hook the plan reaches for really exists in the pinned package — including the three the spike flagged "verify before coding" (`useInterrupt`, `useDefaultRenderTool`, `useCopilotKit`). The blocker (SAN-889 · CK-V2-003 — Host event v2) is **merged on `main` @ `0fab08f`**, so SAN-890 can start.

**What it means in the real world:** Camila's concierge chat — cards, map pins, venue booking, the fast-path that answers "rentals in El Poblado" without waking the agent — is the riskiest thing in this whole migration, and the plan to move it is sound. Nothing ships to Camila until someone sets `COPILOTKIT_V2_CHAT=1`, which stays off on prod. The audit's value: the team can start SAN-890 with confidence the hook map is real, and can **close 3 of the spike's open questions now** instead of re-verifying them mid-build.

| Scorecard | % correct | Dot |
|---|---:|---|
| v1 inventory (every named API in its named file) | **100%** | 🟢 |
| v2 hook map (every replacement exists in `1.55.2/v2`) | **100%** | 🟢 |
| Cliff hooks correctly flagged absent from `/v2` | **100%** | 🟢 |
| `useRenderTool ×12` count accuracy | **100%** | 🟢 |
| Blocker gate (SAN-889 merged) | **100%** | 🟢 |
| Sequencing / flag / VEB-guard design | **95%** | 🟢 |
| File-count baseline (17/8 claimed) | **~85%** (live = **19** react-core · **6** react-ui imports) | 🟡 |
| Open-question freshness (3 now closeable on disk) | **70%** | 🟡 |
| **Overall SAN-890 spec correctness** | **~96%** | 🟢 |

### Legend
🟢 verified true on disk · 🟡 correct but stale/imprecise · 🔴 false or blocking · ⚫ unverifiable here

---

## v1 inventory — verified TRUE (every API is where the spec says)

`git grep` on `main` @ `0fab08f` confirmed each claimed v1 API in its named file:

| File | Spec claims | Disk reality | Dot |
|---|---|---|---|
| `concierge-chat-messages.tsx` | `useCopilotChatInternal`, `useChatContext`, `useCopilotChat` | all 3 present (+ `AssistantMessage`, `CopilotChat`) | 🟢 |
| `concierge-chat-input.tsx` | `useCopilotChatInternal` (interrupt) + fast-path | present | 🟢 |
| `chat-center-panel.tsx` | `CopilotChat` from `react-ui` | present | 🟢 |
| `concierge-assistant-message.tsx` | `AssistantMessage` | present | 🟢 |
| `concierge-coagent-context.tsx` | `useCoAgent` | present | 🟢 |
| `search-tool-renders.tsx` | `useCopilotAction` ×12 (disabled+render) | **12 confirmed** — `useDisabledToolRender` wrapper (L728) × 12 call sites (L732–749) = 6 tools × {Mastra key + createTool id} | 🟢 |
| `concierge-venue-booking-bridge.tsx` | `renderAndWaitForResponse` | present | 🟢 |
| `focus-map-pin-action.tsx` | handler tool → `useFrontendTool` | **`handler:` confirmed** (not disabled+render) → correct mapping | 🟢 |
| `concierge-agent-error-bridge.tsx` | `useCopilotContext` + `setInternalErrorHandler` | both present | 🟢 |
| `event-web-citation-sync.tsx` | `useDefaultTool` | present | 🟢 |
| `chat-filter-copilot-instructions.tsx` | `useCopilotAdditionalInstructions` | present | 🟢 |
| `concierge-session-context.tsx` / `chat-query-bar.tsx` / `concierge-initial-prompt.tsx` / `cafe-detail-panel.tsx` | `useCopilotChat()` (reset / appendMessage) | present | 🟢 |

**The ×12 was the claim most likely to be wrong — it's right.** v1 doesn't loop over a dynamic array (which would have broken a `useRenderTool` migration under Rules of Hooks); it makes 12 fixed wrapper calls, so swapping the wrapper body to `useRenderTool` is a clean 1-edit / 12-call-site change.

---

## v2 hook map — verified TRUE (every replacement is real in 1.55.2)

All present in `node_modules/@copilotkit/react-core/dist/v2/index.d.cts`:

`useAgent · useAgentContext · useFrontendTool · useRenderTool · useHumanInTheLoop · useInterrupt · useDefaultRenderTool · useThreads · useCopilotChatConfiguration · useCopilotKit · useConfigureSuggestions · useSuggestions` — **12/12 OK.**

And the 5 cliff APIs the spec warns about are correctly **absent** from `/v2` (v1-root only):

`useCopilotChatInternal · useChatContext · useCopilotChatHeadless_c · useDefaultTool · useCopilotContext` — **all absent (correct).**

**This is the decisive proof:** the plan's hook map matches the bytes on disk, not just the docs. The chat cliff is real and correctly called out; the replacement hooks all exist.

---

## 🟡 Errors / staleness found (none blocking)

### 🟡 1. Three "verify before coding" open questions are answerable NOW

The spike (§1, §4, §5) and Linear steps A1/A4 say "verify in CopilotKit MCP before coding" for:

| Spike open question | Disk evidence today | Status |
|---|---|---|
| A1 — interrupt replacement for `useCopilotChatInternal` | `useInterrupt` **exists** in `/v2` | **Hook confirmed** — only the *slot-prop shape* still needs a runtime check (see risk R-chat below) |
| A4 — `useDefaultTool` → `useDefaultRenderTool` | `useDefaultRenderTool` **exists** in `/v2` | **Closeable now** |
| A3 — error bridge → `useCopilotKit` / provider `onError` | `useCopilotKit` **exists** in `/v2` | **Hook confirmed**; `onError` prop shape still TBD |

**Fix:** mark A4 ✅ (hook on disk) and A1/A3 "hook confirmed — runtime slot/prop shape pending" in the spike, so SAN-890 doesn't re-spend a half-day re-verifying hook *existence* (that's done) and instead spends it only on the one thing that genuinely needs a running app.

### 🟡 2. File-count baseline drift (17/8 claimed vs 19/6 live)

Linear SAN-890 and `todo.md` cited "17 react-core + 8 react-ui touchpoints." Live `git grep` on `main` @ `0fab08f`, excluding `/v2` and test files: **19** `react-core` · **6** `react-ui` import files (+ `layout.tsx` styles.css). **Fix:** canonical grep pinned in `todo.md` 2026-06-12.

### 🟡 3. "`useRenderTool ×12`" is precise-enough but worth a note

It's 1 `useDisabledToolRender` wrapper hook called 12×, not 12 literal `useRenderTool` calls. The v2 version is the same shape (rewrite the wrapper to call `useRenderTool`). **Fix (optional):** note "1 wrapper hook × 12 call sites" in D1 so the implementer doesn't write 12 separate hooks.

### 🟡 4. Dual representation of the sub-spike (A1–A5)

The A1–A5 sub-spike lives both as a standalone doc (`CK-V2-004-chat-subspike.md`, titled SAN-890A) and embedded in the Linear SAN-890 description. They agree, but two homes will drift. **Fix:** make Linear point at the doc as the single source for the sub-spike appendix.

---

## Genuine remaining risk (correctly known, not an error)

| ID | Risk | Why it's real | Mitigation in spec? |
|---|---|---|---|
| **R-chat-1** | `/v2` `CopilotChat` may not expose `interrupt` on input-slot props the way v1 `useCopilotChatInternal().interrupt` did | Hook *exists* (`useInterrupt`) but the send-gating UX (`canSend = … && !interrupt`) depends on slot-prop shape — only verifiable in a running v2 chat | ✅ flagged as A1 gate — keep it as the **one** true blocker before B-scaffold |
| **R-chat-2** | Fast-path intercept (`sendConciergeUserMessage`) must not migrate into the agent | Camila's "rentals in El Poblado" answers locally without the LLM; moving it would add latency + cost | ✅ §6 invariant — keep `onAgentSend` as the only swapped target |
| **R-chat-3** | VEB routing hijack — day-trip query showing event cards | 12 tool renders + multi-intent routing | ✅ F1 runs `veb-mvp-001` checklist post-migration |
| **R-chat-4** | Map pins must keep syncing (`mapId` on parent `<Map>`) | pins driven by tool renders | ✅ F2 + `mde-maps` |

---

## Per-step correctness (Linear A–G)

| Step group | Verdict | Note |
|---|---|---|
| **A. Sub-spike** | 🟢 95% | A2/A5 done; A1/A3/A4 hook-existence now confirmable on disk — close them |
| **B. Scaffold** (flag + provider gate) | 🟢 100% | Mirrors proven SAN-888/889 flag pattern |
| **C. Agent + context** (`useAgent`, `useAgentContext`) | 🟢 100% | Both hooks verified present |
| **D. Tool renders** (×12 + HITL + map pin + citation) | 🟢 100% | All 4 target hooks verified; ×12 count verified |
| **E. Chat UI** (slots + input + error bridge) | 🟡 90% | Highest risk; gated on R-chat-1 slot-prop check |
| **F. Routing + maps** (VEB guard) | 🟢 100% | Correct guard + `mde-maps` rule |
| **G. Contracts + verify** (flag on/off + floor + e2e + evidence) | 🟢 100% | Matches SAN-888/889 evidence rigor |

---

## Is it 100% correct? — direct answer

**No — ~96%, but every load-bearing claim is verified true and the plan is safe to execute.** The v1 inventory is accurate file-by-file, the v2 hook map matches the installed package exactly, the chat cliff (`useCopilotChatInternal` et al.) is correctly flagged, the `×12` tool-render count is real, and the SAN-889 blocker is merged. The 4% gap is housekeeping: three "verify-first" questions are now answerable from disk, a minor 17/8-vs-19/6 count drift, and one genuine runtime unknown (does `/v2 CopilotChat` surface `interrupt` on slot props?) that *should* stay the gate before any chat-UI code.

---

## Fixes (recommended, to the spike + Linear — no code)

| # | Fix | Where |
|---|---|---|
| 1 | Mark A4 ✅ (`useDefaultRenderTool` on disk); A1/A3 "hook confirmed, slot/prop shape pending" | `CK-V2-004-chat-subspike.md` AC + Linear A-steps |
| 2 | Pin canonical retirement count: **19** react-core / **6** react-ui imports | `todo.md` + SAN-890/891 |
| 3 | Note "1 wrapper hook × 12 call sites" in D1 | spike §7 / Linear D1 |
| 4 | Make Linear reference the spike doc as single source for A1–A5 | Linear SAN-890 |
| 5 | Keep **R-chat-1 (interrupt slot-prop runtime check)** as the explicit A1 gate before B-scaffold | spike + Linear |

**Next step (one thing):** **Do not start SAN-890** until explicit approval. v2 host event is verified on `main` (SAN-889 Done); v1 console noise is **SAN-893**, not a SAN-890 blocker. When approved, apply fixes 1–3 to the spike doc first.

---

## References
- Ground truth: `node_modules/@copilotkit/react-core/dist/v2/index.d.cts` · `package.json` = `1.55.2`
- `git`: `main` @ `0fab08f` (SAN-889 merged — SAN-890 blocker satisfied) · `search-tool-renders.tsx` L728–749 (×12) · `focus-map-pin-action.tsx` `handler:`
- Linear SAN-890 · CK-V2-004 — Backlog · label `V2UP` · blocked-by SAN-889 (merged) · blocks SAN-891
- Spike: [`CK-V2-004-chat-subspike.md`](../tasks/copilotkit/CK-V2-004-chat-subspike.md) · prior audits [`04-copilitkit-audit.md`](./04-copilitkit-audit.md) · [`03-copilotkitv2-upgrade-audit.md`](./03-copilotkitv2-upgrade-audit.md)
