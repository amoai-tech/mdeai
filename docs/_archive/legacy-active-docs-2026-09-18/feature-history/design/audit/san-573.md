**Verdict:** [SAN-573](https://linear.app/sanjiovani/issue/SAN-573) is **~85% correct** — the six components and CLI command match disk + design docs, but acceptance criteria and proof commands are **not shadcn-skill-complete**.

## What’s correct

| Check | Status |
|-------|--------|
| Component list (`tabs`, `command`, `avatar`, `carousel`, `sonner`, `sidebar`) | Matches `design-system.md`, `component-inventory.md`, `design-process.md` §8 |
| CLI | `cd mdeapp && npx shadcn@latest add …` — correct runner + path |
| Registry/style | `components.json` → `style: "base-nova"`, `base: "base"`, `@/components/ui` |
| Gap vs disk | Installed today: 11 primitives; all six targets **missing** (verified via `npx shadcn@latest info --json`) |
| Scope | Install-only primitives for Explore tabs, ⌘K, dashboard shell — right phase (before D-08) |
| Blockers | None on D-07 — correct |

## Errors / gaps (by severity)

### P0 — proof / Done gate will lie or break

1. **`ls` checks only 6 files** — dry-run adds **16 files**, not 6:

   ```
   tabs, avatar, sonner, carousel, command, sidebar
   + textarea, input-group, use-mobile.ts
   ~ button.tsx, skeleton.tsx (OVERWRITE)
   ```

   Done gate should use `npx shadcn@latest info --json` and confirm all six names appear in `components[]`, not a brittle `ls` glob.

2. **Proof re-runs `add` at Done** — wrong per shadcn skill. Done should verify files + build/tests, not re-install (risk of clobbering local edits).

3. **`npm run floor` as hard gate** — repo floor is red for unrelated ESLint noise. Use **`npm run build`** (+ targeted Vitest if button styles regress) as primary gate until floor is fixed.

4. **`button.tsx` / `skeleton.tsx` overwrite** — dry-run will change hover tokens on `Button`. Task must require **`--dry-run` + `--diff`** before apply and a visual smoke on primary buttons (shadcn skill: no silent `--overwrite`).

### P1 — install succeeds but “Done” doesn’t mean usable

5. **`sonner` needs wiring** — generated `sonner.tsx` uses `useTheme()` from **`next-themes`**, which is **not** in `package.json` or `layout.tsx` today. AC “file exists” ≠ toasts work.

   Minimum missing AC:
   - `next-themes` installed (CLI adds it)
   - `<ThemeProvider attribute="class" defaultTheme="light" forcedTheme="light">` (Phase 1 light-only)
   - `<Toaster />` in root layout

6. **New npm deps not in AC** — dry-run adds: `cmdk`, `embla-carousel-react`, `sonner`, `next-themes`. Should verify `package.json` / lockfile.

7. **Post-install review missing** — shadcn skill step 7: read added files, confirm `lucide` icons (not wrong library), no broken composition. Not in task.

### P2 — doc / scope drift (not blocking SAN-573 if scoped as install-only)

8. **`navigation-menu` false ✅** — `component-inventory.md` marks top nav as shipped (`navigation-menu` + `sheet`), but **`navigation-menu` is not installed** and nav is custom (`chat-nav-rail.tsx`). Either add `navigation-menu` to a future task (D-09/D-13 shell) or fix inventory — **not in current P0 list**, which is OK if intentional.

9. **“Wire sidebar / command palette”** — correctly **out of scope** for install-only, but task should say explicitly: *D-07 = add files only; ⌘K / SidebarProvider / CommandDialog wiring lands in D-10/D-13.*

10. **Title says “registry base-nova”** — slightly imprecise. Config is `style: "base-nova"` with `base: "base"` (Base UI primitives, not Radix). Harmless but agents may grep wrong docs path.

## Suggested corrected AC (for 100%)

```markdown
## Pre-flight
- [ ] `cd mdeapp && npx shadcn@latest info --json` — confirm six names absent from `components[]`

## Install (once)
- [ ] `npx shadcn@latest add tabs command avatar carousel sonner sidebar --dry-run` reviewed
- [ ] `--diff button.tsx` + `--diff skeleton.tsx` reviewed before apply
- [ ] Apply add; no manual edits to unrelated UI

## Post-install verify
- [ ] `info --json` lists: tabs, command, avatar, carousel, sonner, sidebar
- [ ] Transitive files present: textarea, input-group, hooks/use-mobile.ts
- [ ] deps in package.json: cmdk, embla-carousel-react, sonner, next-themes
- [ ] Root layout: ThemeProvider (forced light) + `<Toaster />` from `@/components/ui/sonner`
- [ ] `npm run build` exit 0
- [ ] Spot-check Button variants still match design tokens (no gray-* regressions)

## Proof (Done gate — do NOT re-run add)
cd mdeapp && npm run build
npx shadcn@latest info --json | jq '.components'
```

## Score

| Dimension | Score |
|-----------|------:|
| Component selection | 100% |
| CLI / project alignment | 95% |
| Acceptance criteria | 60% |
| Proof commands | 50% |
| shadcn skill compliance | 70% |
| **Overall task spec** | **~85%** |

**Bottom line:** Safe to **start** SAN-573 — the install command is right. Don’t mark Done with current proof alone; fix AC for overwrites, transitive files, deps, and sonner layout wiring first.

Want me to patch the Linear description for SAN-573 with the corrected AC?

**Done** — [SAN-573](https://linear.app/sanjiovani/issue/SAN-573/d-07-p0-shadcn-install-tabscommandavatarcarouselsonnersidebar) description updated.

**Added:**
- Pre-flight (`info --json` before install)
- Install-once flow with `--dry-run` + `--diff` for `button.tsx` / `skeleton.tsx`
- Transitive files + npm deps (`cmdk`, `embla-carousel-react`, `sonner`, `next-themes`)
- Sonner wiring AC (`ThemeProvider` forced light + `<Toaster />`)
- Out-of-scope section (⌘K, sidebar provider, `navigation-menu` → D-09/D-10/D-13)
- Proof commands that **verify only** (no re-run `add`); `build` replaces `floor` as Done gate

**Scope clarified:** install + minimal sonner wiring only — not full shell composition.

**Next:** Execute [SAN-573](https://linear.app/sanjiovani/issue/SAN-573) — it's the only unblocked D-task. Track A soak and D-08 prep can run in parallel.

## Now (P0) — ship D-07

1. **Branch** `ai/san-573-d-07-p0-shadcn-install`
2. **Pre-flight** — `cd mdeapp && npx shadcn@latest info --json` (confirm six absent)
3. **Review overwrites** — `--dry-run`, then `--diff button.tsx` / `--diff skeleton.tsx`
4. **Install** — `npx shadcn@latest add tabs command avatar carousel sonner sidebar`
5. **Wire sonner** — `ThemeProvider` (forced `light`) + `<Toaster />` in `layout.tsx`
6. **Verify** — proof block from Linear AC → `npm run build` → evidence `tasks/testing/evidence/2026-06-05/d-07-shadcn-install.png`
7. **Commit slice** — `feat(ui): P0 shadcn install (SAN-573)` · Linear → **In Review** (not Done until you approve)

~1–2h, low risk, unblocks D-09/D-10/D-13 primitives.

## Parallel (doesn't block D-07)

| Item | Why |
|------|-----|
| **SAN-462 soak** (1/3) | Wait for 2 more scheduled prod synthetics — gates **D-08**, not D-07 |
| **Track A: SAN-478** `/rentals` | D-09 can't skin `/rentals` until functional page exists |
| **Fix `component-inventory.md`** | Mark `navigation-menu` as ☐ not ✅ (doc drift from audit) |
| **Update `san-573.md`** | Add “Linear patched 2026-06-05” + final score ~100% spec |

## After D-07 merges

```
SAN-462 3/3  →  SAN-574 (D-08 VenueCard)  →  parallel SAN-579 (home) + SAN-575 (routes)
                                              →  SAN-577 → SAN-578 → SAN-580
```

**D-08 prep while waiting on soak:** read existing cards (`restaurant-card`, `cafe-result-card`, `rental-card`) and sketch `VenueCard` API — no code until soak clears if you honor the freeze on card refactors.

## Hygiene (optional, 15 min)

- **`track:ux` view** — filter status ≠ Done/Canceled
- **`audit-4-design-tasks.md`** — bump SAN-573 row to “spec 100%, execution ☐”
- **Floor ESLint** — separate infra task; don't block D-07 on it

## What I'd do today

| Priority | Action |
|----------|--------|
| 1 | Execute SAN-573 end-to-end |
| 2 | Let SAN-462 run overnight (passive) |
| 3 | Patch `component-inventory.md` nav row |
| 4 | Start D-08 design spike only after soak 2/3+ |

Want me to **implement SAN-573** (install + layout wiring + commit message draft)?