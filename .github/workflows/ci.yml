# Superseded by floor.yml (PR-16 / SAN-458). Kept for manual re-runs only.
name: CI (legacy)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  ci:
    name: lint · test · build
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - run: echo "Use .github/workflows/floor.yml — npm run floor is the required gate."
